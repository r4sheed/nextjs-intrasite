/**
 * Navigation feature type definitions
 *
 * Shared types, interfaces, and type utilities for the navigation feature.
 * Keep types close to where they're used when possible.
 */

// Core navigation types
export type { NavigationItem } from './navigation-item';
