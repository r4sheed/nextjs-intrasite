/**
 * Posts feature type definitions
 *
 * Shared types, interfaces, and type utilities for the posts feature.
 * Keep types close to where they're used when possible.
 */

// Example domain types:
// export type PostsStatus = 'draft' | 'published' | 'archived';
// 
// export interface PostsFilter {
//   status?: PostsStatus;
//   search?: string;
// }
